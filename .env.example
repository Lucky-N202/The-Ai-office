# Postgres connection string (use pooled URL for serverless, e.g. Vercel Postgres / Neon / Supabase)
DATABASE_URL="postgresql://default:v6hi2zjMEDPW@ep-polished-art-a4l9f1l8.us-east-1.aws.neon.tech/verceldb?sslmode=require&channel_binding=require"
# Direct (non-pooled) URL, required by Prisma for migrations when using a pooler
DIRECT_URL="postgresql://default:v6hi2zjMEDPW@ep-polished-art-a4l9f1l8.us-east-1.aws.neon.tech/verceldb?sslmode=require&channel_binding=require"

# Auth.js
AUTH_SECRET="nRqjoVZkq9sBQbOalqEkzxqJSC/R/urjhKtlT72c3ZI="
AUTH_GITHUB_ID="Ov23liZqI4DkYaaSlWOz"
AUTH_GITHUB_SECRET="03daff050e1d7d174b0fc790adef7ee58cacf508"
NEXTAUTH_URL="http://localhost:3000"

# Site
NEXT_PUBLIC_SITE_URL="https://theaioffice.app"

# Optional. Rate limits the public /submit tool-submission endpoint (5/hour/IP).
# Free tier at upstash.com is enough. If unset, rate limiting is skipped
# entirely rather than breaking the endpoint — safe to deploy without this
# set, just less protected against scripted abuse until you add it.

UPSTASH_REDIS_REST_URL="https://humane-tiger-168990.upstash.io"
UPSTASH_REDIS_REST_TOKEN="gQAAAAAAApQeAAIgcDJhODIzMTdiMTBiMjc0ZDc3OGZmZjFjZGRjMmUzOGU1NQ"

# Intelligence Engine — automated tool-info discovery/verification/enrichment.
# Required for the pipeline to do anything beyond record "content changed":
ANTHROPIC_API_KEY=""
# Optional — defaults to claude-sonnet-5 if unset.
ANTHROPIC_MODEL=""
# Required to protect the cron endpoint from being triggered by anyone who
# finds the URL. Generate with: openssl rand -base64 32
# Set the SAME value here and as a Vercel env var — Vercel Cron automatically
# sends it as "Authorization: Bearer $CRON_SECRET" when calling the route.
CRON_SECRET="4W6YpoiMUNR+vc1+/MVB6iNUQSj6lqYPosj6t1R5EI8="
# Optional — email notifications when a change needs manual review. Without
# these, changes still land in /admin/changes, you just won't get pinged.
RESEND_API_KEY="re_igLa5w8L_PVKfQXqBvBoPRcW31RHagGEg"
ADMIN_NOTIFICATION_EMAIL="theaioffice12@gmail.com"
